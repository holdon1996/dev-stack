import React from 'react';
import { useStore } from '../store';

const Modal = () => {
  // We'll implement modals as needed in the store/actions, 
  // currently the store doesn't have a generic modal state yet.
  // I will add it to the store if needed.
  return null;
};

export default Modal;
